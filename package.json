{
  "name": "axedevhub-sampleproject",
  "version": "1.0.0",
  "description": "Cucumber Framework",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tests_chrome_feature": "npx cypress run --spec cypress/e2e/Features/*.feature --browser chrome",
    "tests_chrome_spec": "npx cypress run --spec cypress/e2e/*.cy.js --browser chrome"
  },
  "keywords": [
    "cypress"
  ],
  "author": "laxminarayana.n",
  "license": "ISC",
  "devDependencies": {
    "@axe-core/watcher": "^3.10.0",
    "cypress": "^13.11.0",
    "cypress-cucumber-preprocessor": "^4.3.1"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": "cypress/e2e/PageObjects/"
  }
}
